import styled from 'styled-components';

const SearchBarStyled = styled.div`

width: 40vw;

  .searchbar {
    width: 100%;
  }

  input:focus {
    border-color: #f17f42;
  }

`;

export default SearchBarStyled;
