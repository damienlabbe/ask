import styled from 'styled-components';

const SearchBarStyled = styled.div`
  input {
    width:20rem;
    height: 2rem;
    font-size: 1em;
    border: none;
    border-radius: .3em;
  }

  input:focus {
    border-color: #f17f42;
  }

`;

export default SearchBarStyled;
