import styled from 'styled-components';

const LoginStyled = styled.div`
  button {
    font-size: 1rem;
    border-radius: 50rem;
    height: 2rem;
    width: 9rem;
  }

`;

export default LoginStyled;
