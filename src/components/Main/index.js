import React from 'react';

import Question from './Question';

const Main = () => (
  <div>
    <button type="submit">New question</button>
    <section>
      <Question />
    </section>
  </div>

);

export default Main;
